# 子目录规则一律带 **/ 前缀，否则只匹配顶层目录。
# 旧项目踩过：只写 __pycache__/ 匹配不到 config/__pycache__，
# 宿主机编译出的 .pyc 把凭据带进了镜像。

# 凭据与运行时数据
.env
.env.local
data/
downloads/
logs/
*.log

# 测试/调试的临时产物（.wb_* / .wb-*）
# 这些是测试脚本在项目目录里留下的临时文件与映射目录。
# v1.0.2 实际发生过：`.wb_s8_data/`（s8 测试的映射目录）被 `COPY . .`
# 带进了镜像，`/app/.wb_s8_data/cookie_state.json` 出现在容器里。
# 内容本身无害（只是「未登录」状态），但这类残留就不该进镜像。
# 另外 tests/s8_check.py 的临时目录也已挪到项目外面，双保险。
.wb_*
.wb-*

# Python 缓存（含子目录）
**/__pycache__/
**/*.py[cod]
**/*.egg-info/
.venv/
venv/
.pytest_cache/

# 版本控制与构建产物
.git/
.gitignore
.github/
Dockerfile
docker-compose.yml
.dockerignore

# 测试脚本不进镜像。
# 它们只在宿主机跑，容器里毫无用处；更重要的是它们会写
# 「要从镜像里确认不存在的凭据片段」这类检查清单 ——
# 一旦打进镜像，校验脚本就会自己命中自己，看起来像凭据泄露。
# （v1.0.1 实际踩过：/app/tests/s8_check.py 里的 SENSITIVE 清单被扫了出来）
tests/
run.ps1
run.bat

# 文档（不影响运行，减小构建上下文）
*.md
!README.md
